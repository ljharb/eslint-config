{
	"name": "@ljharb/eslint-config",
	"version": "5.0.0",
	"author": "Jordan Harband",
	"description": "My shareable eslint config",
	"license": "MIT",
	"main": "index.js",
	"scripts": {
		"prepublish": "in-install || (npm test && eslint-find-rules --unused)",
		"test": "npm run --silent tests-only",
		"posttest": "npm run --silent security",
		"tests-only": "eslint index.js",
		"security": "nsp check"
	},
	"repository": {
		"type": "git",
		"url": "git://github.com/ljharb/eslint-config.git"
	},
	"keywords": [
		"eslint",
		"eslint-config"
	],
	"dependencies": {},
	"devDependencies": {
		"eslint": "^2.13.0",
		"eslint-find-rules": "^1.10.0",
		"nsp": "^2.5.0",
		"replace": "^0.3.0",
		"semver": "^5.2.0",
		"in-publish": "^2.0.0"
	},
	"peerDependencies": {
		"eslint": "^2.13.0"
	},
	"engines": {
		"node": ">= 0.4"
	},
	"publishConfig": {
		"access": "public"
	}
}
